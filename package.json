{
  "name": "pin-generator",
  "version": "1.0.0",
  "description": "4-digit PIN Generator",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p www && cp index.html www/index.html",
    "sync": "npx cap sync android",
    "apk": "npx cap sync android"
  },
  "keywords": [
    "capacitor",
    "android",
    "pin-generator"
  ],
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
